// Get element by index----------
var number = [10, 11, 12, 13, 14, 15, 16];
console.log(number[0]);

var element = number[1];
console.log(element);

// set element value by index----------
number[1] = 1111;
console.log(number);

// find index of an element------------
var positionIndex = number.indexOf(15);
console.log(positionIndex);
